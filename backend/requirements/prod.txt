# ============================================================
# Dépendances de production — 100% GRATUIT (Render + Vercel)
# ============================================================
-r base.txt

# Serveur ASGI (Daphne déjà dans base.txt)
# daphne==4.1.2

# Fichiers statiques
whitenoise[brotli]==6.7.0

# Database URL parser (pour Render PostgreSQL)
dj-database-url==2.3.0

# Pas de Cloudinary (FileSystemStorage gratuit)
# Pas de Sentry (optionnel)
# Pas de Redis (LocMemCache gratuit)
